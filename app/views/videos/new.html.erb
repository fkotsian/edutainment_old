<% provide(:title, "New Video") %>

<h1>Upload your Vid</h1>

<div class="row">
  <div class="span6 offset3">
	<%= form_for([@user, @video]) do |f| %>
	
	  <%= render 'fields', f: f %>
	  <%= f.submit "Create my EDUtainment", class: "btn btn-large btn-primary" %>
	
	<% end %>
  </div>
</div>
